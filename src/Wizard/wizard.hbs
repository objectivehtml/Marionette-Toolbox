{{#if panel}}
<div class="{{panelClassName}}">
    <div class="panel-body">
{{/if}}
        {{#if header}}
            <{{headerTagName}} class="{{headerClassName}}">{{header}}</{{headerTagName}}>
        {{/if}}

        <div class="wizard-progress"></div>

        {{#if description}}
            <p>{{description}}</p>
        {{/if}}

        <div class="wizard-content"></div>

{{#if panel}}
    </div>
    <div class="wizard-buttons"></div>
</div>
{{else}}
<div class="wizard-buttons"></div>
{{/if}}
